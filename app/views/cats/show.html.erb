<h1> <%= "#{@cat.name}" %></h1>

<table border = "1">

  <tr> <td>Name</td> <td><%= "#{@cat.name}" %></td> </tr>
  <tr> <td>Age</td> <td><%= "#{@cat.age}" %></td> </tr>
  <tr> <td>Birthday</td> <td><%= "#{@cat.birth_date}" %></td> </tr>
  <tr> <td>Color</td> <td><%= "#{@cat.color}" %></td> </tr>
  <tr> <td>Sex</td> <td><%= "#{@cat.sex}" %></td> </tr>
</table>

<p>
  <%#= link_to "Cat Edit",  "something (url? direct link?)"%>
  <!-- <a href= <%#= cat_url %>> Edit cat's info </a></p> -->

  <%= link_to "Cat Edit", edit_cat_url %>
